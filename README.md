# Sargam — current project handoff

Start with:

- `CONTEXT.md` — broad project memory and binding product/technical rulings.
- `docs/release-checklist.md` — the current repeatable writing, playback, print, and restoration audit.
- `SARGAM_NEXT_SESSION_CONTEXT_2026-07-30_PRINT_PLAYER_CHECKPOINT.md` — the previous accepted-state history and Library/Queue roadmap.

Older dated handoffs are retained as project history. They are not the current roadmap.

Current stabilization wave: 2026-08-09. **Grid Write** makes the matra grid a direct notation editor: each box writes one matra into the same Markdown source and updates the score and playback immediately, while invalid drafts remain isolated from the composition. Clicking a rendered score matra centers its exact editor cell, and switching between Text Write and Grid Write retains the current line/matra. **Beginning** provides an intentional return to the first line, while **Writing focus** creates a larger split writing surface. The per-note bol menu floats above pane boundaries so every da/ra/diri/chikari choice remains reachable, and **Copy bols** copies a line's editable bol lanes. Exact score anchoring prevents edits and deletions from pushing the working music down. Compact alternate endings use `|1` plus the following notation line: the shared phrase prints once, then real **1st time** and **2nd time** matra grids branch at the exact divergence and remain together when systems wrap. Clean, Matra cells, and Graph Paper share this layout. The score and editor retain persistent **Cells** and **Paper** appearances, with real matra cells rather than a background overlay; PDF export offers the same surface. Verification: **583 checks passed, 0 failed**, and the production build succeeded (the existing large-chunk advisory remains non-blocking).
