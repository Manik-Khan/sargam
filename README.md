# Sargam — current project handoff

Start with:

- `CONTEXT.md` — broad project memory and binding product/technical rulings.
- `docs/release-checklist.md` — the current repeatable writing, playback, print, and restoration audit.
- `SARGAM_NEXT_SESSION_CONTEXT_2026-07-30_PRINT_PLAYER_CHECKPOINT.md` — the previous accepted-state history and Library/Queue roadmap.

Older dated handoffs are retained as project history. They are not the current roadmap.

Current stabilization wave: 2026-08-08. **Grid Write** makes the matra grid a direct notation editor: each box writes one matra into the same Markdown source and updates the score and playback immediately, while invalid drafts remain isolated from the composition. The `+` beneath each exact note attack opens a direct da/ra/diri/chikari menu. Diri now belongs to one note and produces two strokes inside that note's duration, while older bol anchors migrate into editable lanes when Grid Write opens. Dense bol strips stay inside their owning cells, and line repeats occupy dedicated non-musical grid positions so neither can cover notes. The score and editor offer persistent **Cells** and **Paper** appearances; Paper continues each musical row through unused width with empty matra-sized boxes, so the boxes themselves are the graph paper. PDF export offers the same surface. Verification: **578 checks passed, 0 failed**, and the production build succeeded (the existing large-chunk advisory remains non-blocking).
