# Sargam — current project handoff

Start with:

- `CONTEXT.md` — broad project memory and binding product/technical rulings.
- `docs/release-checklist.md` — the current repeatable writing, playback, print, and restoration audit.
- `SARGAM_NEXT_SESSION_CONTEXT_2026-07-30_PRINT_PLAYER_CHECKPOINT.md` — the previous accepted-state history and Library/Queue roadmap.

Older dated handoffs are retained as project history. They are not the current roadmap.

Current stabilization wave: 2026-08-08. **Grid Write** now makes the matra grid a direct notation editor: each box writes one matra into the same Markdown source and updates the score and playback immediately, while invalid drafts remain isolated from the composition. The score and editor offer persistent **Cells** and **Paper** appearances; Paper continues each musical row through unused width with empty matra-sized boxes, so the boxes themselves are the graph paper. PDF export offers the same surface. Kan graces are offset clearly from their destination, and tala markers remain in the cell coordinate lane instead of colliding with vibhag dividers. Verification: **568 checks passed, 0 failed**, and the production build succeeded (the existing large-chunk advisory remains non-blocking).
